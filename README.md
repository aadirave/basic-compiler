To generate the out.cpp file, make a .txt file and compile it from the terminal using python test.py <file>.txt.
Then run g++/your compiler to compile the .cpp file and generate the executable binaries.
